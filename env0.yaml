version: 1

deploy:
  steps:
    terraformInit:
      before:
        - pwd
        - ls -la
        - export
        - if [[ -e $ENV0_TEMPLATE_DIR/.env0/tfinit.sh ]]; then $ENV0_TEMPLATE_DIR/.env0/tfinit.sh; fi