version: 1

deploy:
  steps:
    terraformInit:
      before:
        - pwd
        - ls -la
        - export
        - if [[ -e .env0workdir/.env0/tfinit.sh ]]; then ./.env0workdir/.env0/tfinit.sh; fi